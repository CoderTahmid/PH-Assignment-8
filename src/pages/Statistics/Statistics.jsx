import React from 'react';
// import { useParams } from 'react-router-dom';

const Statistics = () => {

    // const { id } = useParams();
    // console.log(id);

    return (
        <div>
            Statistics
        </div>
    );
};

export default Statistics;